import * as db from '../db/config';

export class Guest {
    private username: string;
    constructor(name: string) {
        
    }
    //
    // async checkForDuplicates(name: string): Promise<boolean> {
    //
    //     return Promise.resolve(false);
    // }

}
